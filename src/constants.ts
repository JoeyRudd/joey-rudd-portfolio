/** File in `public/`; replace file without changing code */
const RESUME_FILE = 'joey-rudd-resume-2026.pdf'

/** Absolute path from site root (includes Vite `base` on GitHub Pages) */
export const RESUME_PATH = `${import.meta.env.BASE_URL}${RESUME_FILE}`
